export interface CrearCliente {
    nombre_completo: string;
    correo_us: string;
    telefono?: string;    // Campo opcional para el teléfono del cliente
    direccion?: string;   // Campo opcional para la dirección del cliente
  }
  